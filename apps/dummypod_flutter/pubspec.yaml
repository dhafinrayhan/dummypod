name: dummypod_flutter
description: A new Flutter project with Serverpod.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  dummypod_client:
    path: ../../packages/dummypod_client
  flextras: ^1.0.0
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.5
  flutter_json_view: ^1.1.4
  gap: ^3.0.1
  serverpod_flutter: 1.2.6

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
